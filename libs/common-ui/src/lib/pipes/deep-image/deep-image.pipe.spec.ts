import { DeepImagePipe } from './deep-image.pipe';

describe('DeepImagePipe', () => {
  it('create an instance', () => {
    const pipe = new DeepImagePipe();
    expect(pipe).toBeTruthy();
  });
});
